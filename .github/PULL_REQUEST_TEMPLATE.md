# Summary
Briefly describe what this PR changes and why. Link any related issue if it exists.

## Type of change
- Bug fix
- New feature
- Breaking change
- Documentation update

## Testing
Commands you ran (if any), e.g. `pixi run pytest ...`, `pixi run ruff check .`, manual steps, or “not tested”.

## Checklist
- [ ] Documentation updated if needed (e.g., `source/openarmlab/docs/README.md`).
- [ ] Tests or checks added/executed as appropriate.
- [ ] Version metadata updated if behavior changes user-facing (`source/openarmlab/config/extension.toml`).
